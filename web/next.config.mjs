import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";

// Enable OpenNext Cloudflare in dev mode
initOpenNextCloudflareForDev();

/** @type {import('next').NextConfig} */
const nextConfig = {
};

export default nextConfig;
