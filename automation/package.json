{
  "name": "arcrouter-content-pipeline",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "dev:scheduled": "wrangler dev --port 8788 --test-scheduled",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.99.0"
  }
}
